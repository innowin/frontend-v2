export type AbilityType = {
  id: number,
  created_time: string,
  updated_time: string,
  delete_flag: boolean,
  title: string,
  text: string,
  ability_organization: number
}

export type AbilityFormType = {
  title: string,
  text: string,
}
