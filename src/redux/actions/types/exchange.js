const SUCCESS = {
  GET_EXCHANGES_BY_MEMBER_IDENTITY: 'GET_EXCHANGES_BY_MEMBER_IDENTITY_SUCCESS'
}

const ERROR = {
  GET_EXCHANGES_BY_MEMBER_IDENTITY: 'GET_EXCHANGES_BY_MEMBER_IDENTITY_ERROR'
}

const BASE = {
  GET_EXCHANGES_BY_MEMBER_IDENTITY:'GET_EXCHANGES_BY_MEMBER_IDENTITY'
}

const IS_LOADING = {
  GET_EXCHANGES_BY_MEMBER_IDENTITY:'GET_EXCHANGES_BY_MEMBER_IDENTITY_IS_LOADING'
}

export default {
  IS_LOADING,
  SUCCESS,
  ERROR,
  BASE
}