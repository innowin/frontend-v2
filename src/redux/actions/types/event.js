const BASE = {
  GET_EVENTS: 'GET_EVENTS',
}

const SUCCESS = {
  GET_EVENTS: 'GET_EVENTS_SUCCESS',
}

const ERROR = {
  GET_EVENTS: 'GET_EVENTS_SUCCESS_ERROR',
}


export default {
  BASE,
  ERROR,
  SUCCESS,
}