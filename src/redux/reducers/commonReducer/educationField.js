import initialState from '../initialState'
import types from '../../actions/types'



const educationField = (state = initialState.common.university, action) => {
  switch (action.type) {
    default:
      return {...state}
  }
}

export default educationField